import React from "react";
import { useTranslation } from "react-i18next";

const RightContent = ({ content }) => {
    return (
        <div class="m_login--right">
            {content}
        </div >
    );
}

export default RightContent;